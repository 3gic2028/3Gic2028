class Main {
  // main method placeholder (JavaScript does not use 'public static void main')
  main(args) {
    let str = "Hello, World!";
    let strLength = str.length;
    for (let i = 0; i < strLength; i++) {
        // loop body here
   <link rel ="stylesheet" href="css colors.css"></link>
   if (str.toLowerCase()) {
     // Example: print each character in lowercase
     console.log(str[i].toLowerCase());
   }
    }
  }
    constructor(parameters) {
        
    }
}